name: Publish Documentation

on:
  push:
    branches:
    - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - run: |
          VERSION=$(date +%s)
          docker login -u baxtergovan -p ${{ secrets.COMMAND_STRING }}
